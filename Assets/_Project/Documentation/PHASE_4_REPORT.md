# Phase 4 Report

Created `DamageInfo`, `IDamageable`, `IHealable`, hitboxes, player health, invincibility, knockback, death, and checkpoint respawn flow. Combat behaviour is composed from focused components.
